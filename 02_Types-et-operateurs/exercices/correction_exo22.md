```c
int main ()
{
    int n, x;
    x = n == 0 ? 0 : (n > 0 ? 1 : -1);
    // ou
    x = n == 0 ? 0 : (n < 0 ? -1 : 1);
    // ou
    x = n > 0 ? 1 : (n == 0 ? 0 : -1);
    // ou
    x = n > 0 ? 1 : (n < 0 ? -1 : 0);
    // ou
    x = n < 0 ? -1 : (n > 0 ? 1 : 0);
    // ou
    x = n < 0 ? -1 : (n == 0 ? 0 : 1);
    // ou
    x = n < 0 ? -1 : n > 0;
    return 0;
}
```